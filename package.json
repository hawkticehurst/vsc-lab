{
	"name": "vsc-lab",
	"version": "0.0.1",
	"private": false,
	"description": "A place for prototyping new UX/UI for VS Code.",
	"author": "Hawk Ticehurst",
	"type": "module",
	"scripts": {
		"build": "node scripts/build.js",
		"deploy": "npm run build && gh-pages -d build",
		"fmt": "oxfmt",
		"fmt:check": "oxfmt --check"
	},
	"devDependencies": {
		"gh-pages": "^6.3.0",
		"oxfmt": "^0.21.0"
	}
}
